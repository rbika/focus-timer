# Copy to `.env` for local development (`cp .env.example .env`).
# Debug builds only — release never loads these files.

# Pin the timer view so it stays visible while you work (skips hide-on-blur).
ALWAYS_ON_TOP=true
